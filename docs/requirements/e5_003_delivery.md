# E5-003 Delivery

- Scope: dominant-scenario resolution and scoring labels for recorded outcomes
- Guarantees:
  - uses one deterministic scoring path for outcome labels
  - supports `hit`, `partial_hit`, `invalidated`, `delayed_realization`, and `wrong_direction`
  - records failure reasons alongside the final score label
