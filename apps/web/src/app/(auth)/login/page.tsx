import { PageCard } from "@/shared/ui/PageCard";

export default function LoginPage() {
  return <PageCard title="Login" description="Reserved for gateway authentication and future session wiring." />;
}
