export type WorldSnapshot = {
  worldId: string;
  tradingDay: string;
  headline: string;
};

export const worldSnapshotStub: WorldSnapshot = {
  worldId: "cn-a",
  tradingDay: "2026-03-12",
  headline: "Market world scaffold is ready for projection data.",
};
