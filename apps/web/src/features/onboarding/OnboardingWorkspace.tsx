import { PageCard } from "@/shared/ui/PageCard";

export function OnboardingWorkspace() {
  return (
    <PageCard
      title="Onboarding"
      description="Statement upload, agent registration, and command-side task submission live here."
    />
  );
}
