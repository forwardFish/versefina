[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "versefina-worker"
version = "0.1.0"
description = "Versefina worker architecture scaffold"
requires-python = ">=3.11"
dependencies = []
